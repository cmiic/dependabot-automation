# Summary

<!-- What does this PR change, and why? -->

## Related issue

<!-- Link the issue this addresses, if any. For non-trivial changes, please open an issue first — see CONTRIBUTING.md. -->

## Checklist

- [ ] `npm run typecheck`, `npm test`, and `npm run lint` pass locally
- [ ] If `src/` or runtime dependencies changed: `npm run build` was run and the updated `dist/` files are committed
- [ ] If behavior, inputs, or outputs changed: the README is updated
